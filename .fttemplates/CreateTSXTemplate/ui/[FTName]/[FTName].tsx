'use client';

import { FC, memo } from 'react';

import { cn } from '@/shared/lib/cn';

interface <FTName | pascalcase>Props {
  className ?: string;
}

export const [FTName | pascalcase]: FC <[FTName | pascalcase]Props> = memo(({ className }) => {
  return (
    <div className={cn('', className)}>
      <FTName | pascalcase>
    </div>
  );
});
